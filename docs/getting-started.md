# Getting Started

## Installation

### macOS

```bash
brew install conductor
```

### Linux

```bash
curl -L https://github.com/conductor/conductor/releases/latest/download/conductor-linux-amd64 -o conductor
chmod +x conductor
sudo mv conductor /usr/local/bin/
```

### Windows

Download the latest release from [GitHub releases](https://github.com/conductor/conductor/releases) and add to your PATH.

## First Workflow

Create a file `hello.yaml`:

```yaml
name: hello
steps:
  - id: greet
    type: llm
    prompt: Say hello to the world in a creative way
```

Run it:

```bash
conductor run hello.yaml
```

You should see a creative greeting generated by your configured LLM.

## Next Steps

Continue to the [tutorial](./tutorial/index.md) to build a complete meal planning workflow.
