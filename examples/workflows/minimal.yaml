# Minimal workflow - demonstrates required input
name: summarize
inputs:
  - name: text
    type: string
    required: true
    description: The text to summarize
steps:
  - id: summarize
    type: llm
    prompt: "Summarize this text: {{.text}}"
